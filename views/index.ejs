<% include partials/header.ejs %>

<div id="map">
</div>
<script>

	var map = L.map('map').setView([47.605017, -122.334437], 11);
	var neighborhood = <%- JSON.stringify(neighborhood) %>;

	  L.tileLayer('http://{s}.tiles.mapbox.com/v3/anniemcblee.8ef08a55/{z}/{x}/{y}.png', {
		     	zoomControl:false,
				attributionControl:false
	  }).addTo(map);


		map.dragging.disable();
		map.touchZoom.disable();
		map.doubleClickZoom.disable();
		map.scrollWheelZoom.disable();

	for (var i = 0; i < neighborhood.length; i++) { 
		L.marker([neighborhood[i].latitude, neighborhood[i].longitude]).bindPopup('<a class="link" href="/' + neighborhood[i].id + '">' + '<h3>' + neighborhood[i].name + '</h3>' + '</a>' + '<h4>' + neighborhood[i].description + '</h4>').addTo(map);
	}; 

</script>

<% include partials/footer.ejs %>